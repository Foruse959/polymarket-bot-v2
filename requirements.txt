# 5min_trade v2.2 Beast Mode — Requirements
# Polymarket V2 (April 2026) — pUSD Collateral
# FIXED: httpx conflict between telegram-bot and clob-client

# Telegram Bot (v21.x uses httpx>=0.27 — compatible with clob-client)
python-telegram-bot>=21.0,<22.0

python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
aiosqlite>=0.19.0

# Polymarket CLOB V2 SDK (REQUIRED for live trading)
py-clob-client-v2>=0.1.0

# Ethereum / Web3 (pUSD on-chain balance reads)
eth-account>=0.10.0
web3>=6.0.0
eth-abi>=4.0.0

# Builder relayer (gasless + deposit wallets, optional)
py-builder-relayer-client>=0.0.1
py-builder-signing-sdk>=0.0.1

# Data analysis (optional — only for backtest)
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=12.0.0
